# Target: dev-backend
# Merges: base.common → env.dev → component.backend
classes:
  - env.dev
  - component.backend

parameters:
  kapitan:
    vars:
      target: dev-backend
    compile:
      - input_paths:
          - templates/manifests.yml
        input_type: jinja2
        output_path: .
        output_type: yaml
